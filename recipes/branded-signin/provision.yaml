# branded-signin — how to get Thoryn up and running for this example (a branded hosted sign-in screen).
# SSO-3092 (epic SSO-3087): this is the file a customer copies into their own `.thoryn/` and converges with
# `thoryn provision apply`; `thoryn examples apply branded-signin` converges it FIRST (the recipe's params feed the
# `{{env.NAME}}` / `*Env` placeholders below), then runs the recipe's extra steps. Closed `kind` allowlist
# (schema/provision.schema.json); secrets only as env-var NAMES.
apiVersion: thoryn.io/provision/v1
resources:
  - kind: environment
    name: sandbox
    spec:
      slug: "{{env.envSlug}}"
      displayName: "branded-signin example"

  - kind: application
    name: rp
    environment: sandbox
    spec:
      displayName: "{{env.appName}}"
      clientType: public
      grantTypes: ["authorization_code"]
      scopes: ["openid", "profile", "email"]
      redirectUris: ["http://127.0.0.1/callback"]
      # RFC 8252 loopback (port-agnostic) + OIDC RP-Initiated Logout post-logout URI (SSO-2897).
      postLogoutRedirectUris: ["http://127.0.0.1/"]

  - kind: user
    name: demo
    environment: sandbox
    spec:
      # The demo account `thoryn examples run` (and the CI journey) signs in as. The password comes from
      # the env var / recipe param named here — never written to this file or any receipt.
      email: "{{env.envSlug}}@example.com"
      passwordEnv: DEMO_USER_PASSWORD
      givenName: "Demo"
      familyName: "User"
      emailVerified: true

  - kind: loginTheme
    environment: sandbox
    spec:
      # Look-and-feel of the hosted sign-in / register screens (SSO-3037/3038).
      primaryColor: "{{env.primaryColor}}"
      backgroundColor: "#ffffff"
      borderRadiusPx: 10
      theme: "{{env.loginTheme}}"
      cssVariables:
        "--thoryn-accent": "#0369a1"
        "--thoryn-font-family": "Georgia, serif"
