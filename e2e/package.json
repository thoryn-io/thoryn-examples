{
  "name": "@thoryn/example-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "SSO-2909 — Playwright harness for the simple-signin example: a real browser Path-B journey (self-service sign-up -> captured verification email -> verify -> sign in through the recipe's loopback RP -> protected page) against the browsable published stack. SCAFFOLD — see docs/ci-e2e-plan.md.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:unit": "node --test lib/mailpit.parse.test.mjs",
    "typecheck": "tsc --noEmit",
    "install-browser": "playwright install --with-deps chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.48.2",
    "@types/node": "20.14.10",
    "typescript": "5.6.3"
  }
}
