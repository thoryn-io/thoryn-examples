# magic-link-signin — E2E results

_Pending the first live run._ This file is generated from the actual run by
`e2e/lib/e2e-results-reporter.mjs` when the `magic-link-signin` Playwright project executes
(dispatch `magic-link-signin-e2e.yml`). Until then it is a placeholder — the recipe ships
dispatch-only so the first live run can confirm the seams before the nightly adopts it.
