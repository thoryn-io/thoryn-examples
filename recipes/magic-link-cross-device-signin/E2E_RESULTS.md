# magic-link-cross-device-signin — E2E results

_Pending the first live run._ Generated from the actual run by `e2e/lib/e2e-results-reporter.mjs`
when the `magic-link-cross-device-signin` Playwright project executes (dispatch
`magic-link-cross-device-e2e.yml`). Ships dispatch-only so the first live run can confirm the seams
before the nightly adopts it.
