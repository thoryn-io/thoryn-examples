# E2E results — `magic-link-signin`

<!-- GENERATED FILE — do not edit by hand. Regenerated by the shared Playwright
     reporter (e2e/lib/e2e-results-reporter.mjs) on every live run, and seeded by
     e2e/scripts/init-e2e-results.mjs (`npm run results:init`). -->

**E2E coverage:** yes — colocated at [`e2e/`](./e2e/).
**Runs in CI via:** `.github/workflows/magic-link-signin-e2e.yml` (nightly + `workflow_dispatch`).
**Latest result:** ✅ passed
**Tests:** 2 passed · 0 failed · 0 skipped (of 2).
**Generated:** 2026-09-22T06:39:47.719Z — by the Playwright reporter (e2e/lib/e2e-results-reporter.mjs).

## Scenario

**Passwordless (magic link), same device: request a link, open it here, you're signed in**

Drives the `magic-link-signin` recipe's loopback relying party against the staging SaaS in a real browser, pointed at a FRESH per-run SANDBOX ENVIRONMENT (env.create). A verified user signs in with NO password: on the hosted login they choose 'email me a sign-in link' (identity POST /auth/magic-link/request), the email is captured from the per-run sandbox test inbox (suppressed from real SMTP by TestModeEmailGate, channel magic_link, read via `thoryn env test-emails`), and opening that single-use link ON THE SAME device — the one that started the sign-in, carrying the ML_INIT cookie — authenticates the session and resumes /oauth2/authorize to the RP protected page. A bogus/expired token is rejected. The whole sandbox (client + user) is hard-deleted on teardown. Magic-link is on by default, so no login-method recipe action is needed.

## Steps / assertions

| # | Step | Result |
|---|------|--------|
| 1 | Request a passwordless sign-in link on the hosted login (sets the ML_INIT device cookie) | ✅ passed |
| 2 | Capture the single-use magic link from the sandbox test inbox (channel magic_link) | ✅ passed |
| 3 | Open the link on the SAME device → authenticated → OAuth resumes → /protected | ✅ passed |

**Error path also covered:** An invalid/expired magic-link token does not sign the user in

## Tests

| Test | Result | Duration |
|------|--------|----------|
| full journey: request a magic link → open it on the same device → /protected | ✅ passed | 9.1s |
| error path: an invalid/expired magic link does not sign the user in | ✅ passed | 6.5s |

## Environment

| Key | Value |
|-----|-------|
| Issuer | examples.hub.stg.thoryn.org/ci-magic-link-signin |
| Relying party | http://127.0.0.1:8471 |
| Identity host | https://identity.stg.thoryn.org |

## Links

- [CI run](https://github.com/thoryn-io/thoryn-examples/actions/runs/35695752618)
- [Playwright HTML report (CI artifact)](the `magic-link-signin-e2e-playwright-report` artifact on the CI run)

